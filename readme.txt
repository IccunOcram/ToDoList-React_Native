Per far partire l'applicazione aprire la cartella della stessa usando il comando cd
Successivamente installare le dipendenze con yarn install (per installare yarn seguire il link https://yarnpkg.com/lang/en/docs/install/)
Successivamente per far partire l'applicazione usare il comando yarn start o expo start